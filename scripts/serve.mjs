import http from 'node:http';import fs from 'node:fs/promises';import path from 'node:path';
const root=path.resolve(process.argv.includes('--package')?'tmp/package-check':'dist');const port=Number(process.env.PORT||4180);
const types={'.html':'text/html','.js':'text/javascript','.css':'text/css','.png':'image/png','.svg':'image/svg+xml','.json':'application/json'};
http.createServer(async(req,res)=>{try{const pathname=decodeURIComponent(new URL(req.url,'http://local').pathname);const marketing=!process.argv.includes('--package')&&pathname.startsWith('/marketing/');const base=marketing?path.resolve('marketing'):root;let file=path.resolve(base,'.'+(marketing?pathname.slice('/marketing'.length):pathname));if(!file.startsWith(base+path.sep)&&file!==base)throw Error();if(file===base)file=path.join(base,'index.html');let content=await fs.readFile(file);res.writeHead(200,{'Content-Type':types[path.extname(file)]||'application/octet-stream','Cache-Control':'no-store'});res.end(content);}catch{res.writeHead(404);res.end('Not found');}}).listen(port,'127.0.0.1',()=>console.log(`Little Vac: http://127.0.0.1:${port}`));
